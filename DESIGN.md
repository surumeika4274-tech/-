# 設計仕様書（v5・最終調整版）

本書はゲーム内の全パラメータ・ルール・判断根拠を一箇所に集約したもの。数値の実体は `js/data.js`（`PARAMS`・各テーブル）、編構成は `js/story.js`、カード表は `js/cards.js`（`tools/build_cards.js` で生成）にある。

## 0. 本作の骨格（『ブルーロックPWC』との関係）

| 項目 | ブルーロックPWC | 本作 |
| --- | --- | --- |
| 育成の回数 | 1回（入寮〜数十週で完結） | **5回**（五編：一次選考／二次選考／三次選考／新英雄大戦／U-20 W杯）。同一選手を編ごとに育成し直し、編末に査定 |
| 育成方針 | — | 編の開始時に 4 方針から選択（成長補正） |
| レアリティ | ★1〜★5 ＋ ★4FLOW | 8段階（提供割合 0.2/0.8/2/5/12/25/30/25%） |
| ピース／星上げ | ピースで星上げ | エゴ・ピース（再排出時に獲得、交換所で任意カードと交換） |
| 失敗 | やり直し可 | 不可逆オートセーブ。除籍＝RUN 終了、補償ジェムのみ持ち越し |
| 終着点 | 育成完了で評価 | 世界一 → 殿堂入り → FIFA ワールドカップ（if） |

拡張性：`js/story.js` の `arcs` 配列に編オブジェクトを追加するだけで編数を増やせる（`n`・`part`・`envMult`・`segments`・`cut` を持つ）。編基準値 `RANK_PAR` と実績 `five_parts` の参照は末尾要素にクランプされる。

## 1. データ源と確定事項

| 事項 | 確定内容 | 根拠／状態 |
| --- | --- | --- |
| 選手カード（PWC） | 41キャラ170枚をカード名・タイプ・ポジション付きで収録 | Game8 全キャラ一覧の HTML から機械抽出（`data/pwc_cards.json`） |
| 選手カード（改変版オリジナル） | 35キャラ75枚。PWC 未収録キャラをストーリー段階別に複数個体化 | 本作の設計。`stage` フィールドに段階名 |
| レアリティ | 8段階・提供割合 0.2/0.8/2/5/12/25/30/25% | 仕様書 §3 |
| PWC カードの8段階割り当て | キャラの原作の格 (floor, peak) と PWC レアリティで内挿 | `tools/build_cards.js` の `BAND`。序列は本作の判断 |
| PWC 現行の ★4/★4FLOW/★5 提供割合 | 8段階に置き換えたため参照しない | 公開数値は確認できなかった（未検証事項として記録） |
| 原作の試合結果 | 一次〜NEL・W杯GL の各スコア・得点者 | README の出典を参照。一次選考の各スコアは2ソース一致 |
| W杯イングランド戦 | 原作連載中 | 本作は「勝利以外は敗退」として実装 |
| W杯決勝T・FIFA W杯 | 原作未到達の if | ゲーム内に「if」と明記 |
| キャラ画像 | 原作画像は同梱しない | 著作物のため（個人使用でも GitHub／claude.ai 経由の配布は個人使用の範囲外）。生成 SVG アート＋利用者画像の取り込み（単体／一括）で代替 |

## 2. カードとステータス

- 初期値 `base[s] = R.base × ident[s] × (主タイプ 1.3 / 他 0.95) × (1 + 0.05 × 限界突破)`
- 成長補正 `growth[s] = R.growth × (0.7 + 0.3 × ident[s]) × (主タイプ 1.12)`。RUN 中はさらに `× (1 + 育成方針[s] + アドバイザー[s])`
- レア基準値 R.base / R.growth：★1 24/0.90、★2 30/0.98、★3 38/1.06、★4 47/1.15、★5 58/1.25、★6 70/1.36、★7 84/1.48、★8 100/1.62
- タイプ→主属性：キック→SHT、スピード→SPD、テクニック→TEC、賢さ→INT、フィジカル→PHY、スタミナ→PHY（練習HP消費 −2）、コンディション→INT（不調に落ちない・休養 +10）
- ポジション適性：第1ポジションに応じて Option へ +2%（CF/CB→A、OMF/RMF/LMF→B、DMF→D、RWG/LWG/RSB/LSB→C）
- ★4FLOW 由来：FLOW 突入率 +15%、FLOW ボーナス +5pt
- 固有エゴ（キャラ共通の常時効果）と固有覚醒スキル（主属性 ≥ 220×閾値倍率で成功、または FLOW 成功で覚醒。FLOW 成功で Lv+1）
- 限界突破：同一カード再排出ごとに初期値 +5%・成長 +3%、上限なし、周回を跨いで永続
- エゴ・ピース：再排出時に段階別に獲得（★1 2 / ★2 3 / ★3 5 / ★4 10 / ★5 20 / ★6 40 / ★7 80 / ★8 150）。交換所の価格 ★1 30 / ★2 50 / ★3 90 / ★4 200 / ★5 400 / ★6 800 / ★7 1,600 / ★8 3,200。交換は所持済みカードにも可能（限界突破 +1）

## 3. 育成（1クリック = 1週、全55週）

- 編の開始時に育成方針を選択：決定力特化（SHT・PHY +12%、他 −4%）／戦術特化（INT・TEC +12%）／快速特化（SPD・INT +12%）／バランス（全 +4%）。編ごとにリセットされ、選び直す
- 練習：`gain = (基礎 10 × 編環境倍率 + 現在値 × 0.006) × (1 + 0.05 × スキル数) × 成長補正 × (1 + 0.03 × 限界突破) × HP効率 × コンディション × 化学反応` ／ 副次属性は基礎 2・複利 0.002
- 編環境倍率：第一編 1 / 第二編 2.5 / 第三編 6 / 第四編 12 / 第五編 26
- おまかせ練習：次の試合で最も比率の高い選択肢を構成する 2 属性のうち、今週の獲得量が大きい方を選ぶ（`BL.recommendStat`）。判断を省くだけで有利にはならない
- HP：練習 15±2（補正後最低 5）消費、休養 +40。HP≥50 効率1.0、30〜49 効率0.5、<30 効率0.35 かつ故障率 40%（固有エゴ・スキルで軽減、下限 5%）→ 故障は即除籍
- コンディション：絶好調 ×1.15（HP消費 −1）／好調 ×1.05／普通 ×1.0／不調 ×0.85（HP消費 +2）。練習後 15% で低下・12% で上昇、休養後 60% で上昇
- 化学反応練習：毎週ランダムに1属性を指定、その練習の主獲得量 ×1.25
- 突発化学反応イベント：練習後 20%（固有エゴで加算）。28種、同キャラのイベントは除外、同一イベントの連続を回避。ギャンブル選択肢は成功率明示
- 購買部：価格 = 基本価格 × 編番号 × アドバイザー係数。カプセル 40,000（HP 100）、プロテイン 35,000（次回練習 ×2、重複不可）、ノート 90,000（次試合全選択肢 +10%、重複不可）。週を消費しない
- 誤連打ガード 180ms（確認ダイアログは置かない）

## 4. 試合（Climax）

- 選択肢：A = SHT+PHY、B = INT+TEC、C = SPD+INT、D = INT+TEC（INT系 T2 以上のスキル所持で出現、レートは B より低め）。単発試練（オニごっこ・100ゴール）は専用3択
- 比率 `r = パワー ÷ 敵レート`。`r < 0.7` → 0%（補正無効）。基礎 `= sigmoid(6 × (r − 1))`
- 乗算補正（失敗率の割合削減）：スキル合算（上限 25）＋固有エゴ＋アドバイザー＋クラブ＋ポジション適性＋アナライズノート 10
- FLOW：最良選択肢が 25〜40% のとき 25%（＋固有エゴ・アドバイザー・FLOW カード）で突入、全選択肢 +20pt（FLOW カードは +25pt）、成功時スキル確定覚醒
- 成功率上限 92%（単発試練は 100% まで）
- 判定はクリック瞬間に乱数で確定し保存。提示済みの選択肢・FLOW 状態もリロードで再抽選されない
- スキル覚醒：成功時、選択肢の対応系統で閾値（60/150/400/1000/2500 × 閾値倍率）を満たす最上位を覚醒。既所持スキルの重複は 35%（FLOW 成功時は確定）
- コールド：勝利必須ルールで必要成功数の達成が数学的に不可能になった瞬間に打ち切り。同点は即時敗北（4 Climax の FIFA W杯で発生）
- ゴール時はカットイン演出（`prefers-reduced-motion` で無効）

## 5. 編構成・ルール・足切り

| 編 | 節（週） | ルール | 編末 |
| --- | --- | --- | --- |
| 第一編 一次選考（12） | オニごっこ（0・単発）→ X(3) → Y(3) → W(3) → V(3) | single（失敗＝脱落）→ league（試合単位の除籍なし） | 合計 280 ／ 2勝以上 ／ 最終戦勝利（不可能になった瞬間に除籍） |
| 第二編 二次選考（11） | 100ゴール(2・単発) → 凛(3) → [敗北時] 2v2(0) → 國神・千切・玲王(3) → 4th 凛(3) | stage3_rin：敗北で分岐 ／ mustWin ／ stage4_rin：敗北時 INT ≥ 420 or 2得点で指名 | 合計 700 |
| 第三編 三次選考（8） | トライアウト(4) → U-20戦(4) | tryout / mustWin | 合計 1,800 |
| 第四編 新英雄大戦（12） | クラブ選択 → 4戦(各3) | nel（除籍なし・対戦相手は残り4クラブ） | 累積年俸 1.6億円 |
| 第五編・決戦 U-20 W杯（12） | GL 3戦(各2) → QF → SF → 決勝(各2) | group（3戦2勝）／ mustWin ／ perfect | 世界一で殿堂入り |

- 編評価ランク：`合計ステータス ÷ 編基準値[170, 700, 2,500, 6,000, 20,000]` で SS ≥ 3.0 / S ≥ 2.2 / A ≥ 1.6 / B ≥ 1.2 / C ≥ 0.9 / D。RUN 履歴に 5 編分を記録。SS で実績
- 除籍時の補償ジェム：編番号 × 300（最低 100、絵心アドバイザー ×1.5）。生存：+500、世界一：さらに +2000
- 年俸単価（ゴールあたり）：第一編 30万 ／ 第二編 80万 ／ 第三編 200万 ／ 第四編 600万 ／ 第五編 1,500万。倍率 `(1 + 0.04 × スキル数 + スピード系加算) × 固有エゴ × 圧倒度 (1 + 0.25 × min(2, r − 1)) × MVP 1.5 × FLOW成功 1.2`（r はその試合の最良選択肢の比率。FLOW 帯に入らないほど強い選手が年俸で不利にならないための補正）。原作の入札上位（凛・潔 2.4億）が ★7 クラスの新英雄大戦終了時の中央値〜上位に相当する尺度
- Cash（ゴールあたり／勝利ボーナス）：2万／4万／8万／15万／25万
- 敵レートは `tools/power.js` で計測した ★3 の中央値を基準に、序盤 r≈1.3 → 決勝 r≈0.72 となるよう設定し、編ごとに 10〜14% 引き下げたのち、v5 の育成方針・週数増に合わせて第一編 ×1.08・第二〜四編 ×1.05・第五編 ×1.13 に再調整した値（`js/story.js`）

## 6. ローグライト（メタ）

- 初期：★1 潔世一【チームＺ】と 1,500 Ego Gems。スカウト 単発 150 / 10連 1,500。天井なし（エゴ・ピースが救済）
- アドバイザー：絵心（閾値 ×0.9・補償 ×1.5）、アンリ（HP消費 −2・休養 +5）は初期解放。ノア（全 +3%）＝U-20戦勝利、冴（B +4%・TEC 成長 +10%）＝二次選考突破、三笘（C +4%・SPD 成長 +10%）＝10周目、ロキ（FLOW +8%・FLOW 成功時年俸 ×1.3）＝NEL 突破、不乱蔦（Cash ×1.5・価格 ×0.85）＝年俸1億
- 実績 27 種（合計 9,400 ジェム）。RUN 履歴 20 件、BLランキング `300 × (編基準値 ÷ 合計)²`
- 殿堂入り：世界一達成時に能力・スキル・固有覚醒・アドバイザー・クラブを保存。FIFA W杯（アルゼンチン→ブラジル→イングランド→ノア率いるフランス、4 Climax・3勝以上・一発勝負）
- 新規 RUN への能力持ち越しは無し。セーブは `localStorage`（`bl_pwc_egoist_save_v4`）。行動ごとに上書き、リセット不可。利用者画像は同キーの `meta.portraits` に保存（1枚 60KB・合計 3MB 上限）

## 7. 曖昧・未確定だった事項の確定判断

1. **引き分け即死**：3 Climax 制では発生しないため、FIFA W杯を 4 Climax・3勝必要とし 2-2 を即時敗北にして機能させた。
2. **第一編の足切り**：仕様の「合計ステータス規定値 ＆ 1ゴール」を、原作の総当たり（2勝1分1敗・2位）に合わせ「2勝以上 ＆ 最終戦勝利」に拡張。1ゴールは自動的に含意される。入寮テストは独立章ではなく第一編の先頭節（0週）。
3. **第二編の敗北**：原作どおり凛戦の敗北は除籍ではなく「奪われる」分岐。4th 敗北時の指名条件は INT 420 または 2得点。
4. **第四編の年俸線**：原作の 24位（凪 2,400万円）は本作の累積年俸と一致しないため、計測した分布（到達者のうち ★4 の約 2 割・★3 の約 3 割が落ちる水準）から 1.6億円に設定。v4 までの 5億円は年俸尺度が原作の 10 倍だったため撤回し、単価を 1/10 に改めた。
5. **第五編の対戦相手**：GL は原作どおり（イングランド戦は結果未確定のため勝利必須）、決勝T は if（ドイツ＝カイザー／スペイン／フランス＝ロキ）。
6. **★8 の飽和**：成功率上限 92% を導入（単発試練は除外）。理論上限は決勝 perfect 0.92³ × 準決勝・準々決勝 ≈ 75%（第五編突入時点から）。
7. **★1 初期配布の難度**：一次選考突破 約25%。仕様の「生存縛り」に従い緩和しない。
8. **スキル倍率**：指数（1.05^n）は終盤で発散するため線形（1 + 0.05n）。重複覚醒 35%。
9. **PWC 提供割合**：8段階採用により参照不要。PWC 準拠値を使う場合は `RARITY[*].rate` を書き換える。
10. **画像**：原作画像は権利上同梱不可。生成 SVG（タイプ色・レア枠・姓の頭文字エンブレム・シルエット、個体差の傾き／反転、★7 以上ホロ）＋利用者画像取り込み（単体／ファイル名判定の一括）で対応。
11. **育成方針の効き**：+12% は 12 週の編で主属性 +12% 程度の差。編末足切りの合計ステータスには「バランス」が有利、試合の特定選択肢には特化が有利という二択にした。
12. **シミュレータの方針選択**：`tools/sim.js` はカードの主タイプに沿った方針を選ぶ（人間の判断に合わせる）。`POLICY=balance` 等で固定可。

## 8. 既知の限界

- バランスは貪欲 AI の推定値（40試行/カード、同レア同キャラは1枚）。人間の判断（年俸管理・アイテム・化学反応の活用）で上振れする。
- 原作の各選手の能力序列（`BAND`・`ident`）は本作の設計判断であり、原作に明示された数値はない。
- 海外選手の一部（ライデン等）は原作の描写が少なく、能力は役割からの推定。
- 新英雄大戦の年俸線 1.6億は本作の分布に基づく値で、原作の 23 位の金額を再現したものではない（原作の当該金額は未確認）。
